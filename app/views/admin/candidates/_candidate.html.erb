<p>
  <% @candidates = User.where(:role=> "candidate")%>
  <table>
    <thead>
      <tr>
        <th>First Name</th>
        <th>Last Name</th>
        <th>Email </th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
      <% @candidates.each do |candidate|%>
      <tr>
        <td><%= link_to candidate.first_name, admin_candidate_jobs_path(candidate) %></td>
        <td><%= candidate.first_name %></td>
        <td><%= candidate.email %></td>
        <td><%= link_to "view","/admin/users/#{candidate.id}"%> 
      </tr>
      <% end %>
    </tbody>
      
  </table>
</p>
